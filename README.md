# Fig.3 실험 재현: FLTG vs FLTrust (CIFAR-10)

본 레포지토리는 연합학습(Federated Learning) 환경에서  
**FLTG와 FLTrust 집계 알고리즘의 공격 내성(robustness)**을 비교하기 위해  
논문 Fig.3 스타일의 실험을 재현한 코드이다.

CIFAR-10 데이터셋을 사용하며,  
데이터 분포 편향(bias)과 악성 클라이언트 비율 변화에 따른 성능을 분석한다.

---

## 📌 실험 개요

- **데이터셋**: CIFAR-10  
- **모델**: Simple CIFAR CNN (`simplecifar`)  
- **클라이언트 수**: 100  
- **공격 방식**: Min-Max 공격  
- **집계 알고리즘**
  - FLTG
  - FLTrust
  - FedAvg (공격 없는 기준선)

본 실험의 목적은  
**악성 클라이언트 비율이 증가하더라도 FLTG가 안정적인 성능을 유지하는지**를  
FLTrust 및 FedAvg와 비교하여 확인하는 것이다.

---

## ⚙️ 실행 환경

- Python 3.x
- PyTorch (main.py 기준)
- pandas
- matplotlib

필요 라이브러리 설치:

```bash
pip install pandas matplotlib
🧪 실험 설정
기본 파라미터
항목	값
데이터셋	CIFAR-10
모델	simplecifar
클라이언트 수	100
글로벌 에폭	5
배치 크기	64
반복 횟수	1
GPU	사용 안 함 (gpu_id = -1)

데이터 편향(bias) 설정
python
코드 복사
BIAS_LIST = [0.1, 0.5, 0.8]
악성 클라이언트 비율
python
코드 복사
TRAITOR_LIST = [0.2, 0.5, 0.8, 0.95]
🚀 실험 실행 방법
아래 명령어를 실행하면 Fig.3 실험이 전체 자동으로 수행된다.

bash
코드 복사
python run_fig3.py
해당 스크립트는 다음을 수행한다.

bias 확률, 악성 클라이언트 비율, 집계 알고리즘 조합별 실험 실행

각 실험 로그 저장

최종 테스트 정확도를 CSV 파일로 저장

📂 결과 파일 구조
실험이 완료되면 아래와 같은 파일들이 생성된다.

bash
코드 복사
logs_fig3_cifar10_client100_simplecifar_epoch5/
 └── bias{bias}_traitor{비율}_{aggr}.log

results_fig3_cifar10_client100_simplecifar_epoch5.csv

Results_fig3_cifar10_client100_simplecifar_epoch5/
 └── (각 실험 결과 npy 파일)
CSV 파일 형식
text
코드 복사
bias_prob, traitor, aggr, test_acc
test_acc는 0~1 스케일로 저장됨

📊 Fig.3 결과 그래프 생성
실험 결과를 논문 Fig.3 스타일로 시각화하려면 다음을 실행한다.

bash
코드 복사
python plot_fig3.py
그래프 구성
X축: 악성 클라이언트 비율 (%)

Y축: 테스트 정확도 (%)

서브플롯: bias 확률별 분리

기준선: 공격이 없는 FedAvg (점선)

📈 결과 해석
악성 클라이언트 비율이 증가함에 따라
FLTrust는 성능 저하가 발생하는 반면,

FLTG는 비교적 안정적인 정확도를 유지함을 확인할 수 있다.

FedAvg는 공격 환경에서는 사용하지 않으며,
공격이 없는 기준 성능으로만 활용된다.

📝 참고 사항
정확도는 로그 출력에서 자동으로 추출된다.

글로벌 에폭 수를 증가시키면(예: 20~50) 논문 결과와 더 유사한 경향을 확인할 수 있다.

본 코드는 논문 실험 구조 이해 및 재현을 목적으로 작성되었다.

yaml
코드 복사
